## What this changes

## Why

## How it was verified

- [ ] `make test` passes (backend and frontend)
- [ ] `make lint` and `make typecheck` pass
- [ ] `make security` passes (bandit, pip-audit, npm audit)
- [ ] Ran the affected surface locally, not only the tests

## Notes for the reviewer

Anything surprising, any tradeoff you made, anything you deliberately left out.
